<% content_for :body_content_left do %>
  <ul id="portfolios">
    <% @portfolios.each do |portfolio| %>
      <li>
        <%= link_to portfolio.name, portfolio_url(portfolio) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
