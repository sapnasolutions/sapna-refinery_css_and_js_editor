<% content_for :body_content_title do %>
  <%= @portfolio.name %>
<% end %>

<% content_for :body_content_left do %>
  <section>
    <h1>Name</h1>
    <p>
      <%=raw @portfolio.name %>
    </p>
  </section>
  <section>
    <h1>Url</h1>
    <p>
      <%=raw @portfolio.url %>
    </p>
  </section>
  <section>
    <h1>Photo</h1>
    <p>
      <%= image_fu @portfolio.photo, nil %>
    </p>
  </section>
  <section>
    <h1>Description</h1>
    <p>
      <%=raw @portfolio.description %>
    </p>
  </section>
<% end %>

<% content_for :body_content_right do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="portfolios">
      <% @portfolios.each do |portfolio| %>
        <li>
          <%= link_to portfolio.name, portfolio_url(portfolio) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render :partial => "/shared/content_page" %>
